# 项目永久规则

## 文件修改安全规则

- 严禁对项目源码文件使用整文件覆盖式写入。
- 修改现有源码文件前，必须先读取目标文件的相关上下文。
- 修改现有源码文件时，只能使用局部替换、补丁式修改或最小范围编辑。
- 尤其禁止对 `src/app/page.tsx` 使用整文件写入，因为该文件体积大且承载主页面逻辑，整文件写入会导致项目代码被截断或丢失。
- 如果确实需要大范围重构，必须先向用户说明风险并获得明确确认。
- 修改后必须检查关键片段，并尽可能运行构建或类型检查验证项目没有被破坏。
